import React from 'react';

// Living film-grain that sits over the whole page (CSS-driven, pointer-through).
export function GrainOverlay() {
  return <div className="grain" aria-hidden="true" />;
}
